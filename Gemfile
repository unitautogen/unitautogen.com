source "https://rubygems.org"

# Same Jekyll + plugin versions GitHub Pages uses, so a local preview matches production.
# Only needed for `bundle exec jekyll serve`; GitHub Pages ignores this file and builds on its own.
gem "github-pages", group: :jekyll_plugins
